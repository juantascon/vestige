preguntar que pasó al fin con lo de los disenos

modificar la forma de visualizar las listas, la lista debe cubrir todos los elementos excepto el último

dibujar todos los modelos con punto de coordenadas en la mitad (ej los labels deben estar centrados)

buscar porque al resolver un problema es mejor volver a empezar que devolverse un paso

reconocer cual es el dato de entrada y cual es el de salida (I/O)

generar num aleatorios para el valor de cada bloque
cambiar las reglas para que permitan pop o push del ultimo elemento sin importar quien es el elemento
identificar visualmente las pilas de entrada y salida

